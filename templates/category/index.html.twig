{% extends 'base.html.twig' %}

    {% block title%}
        {{ title }}
    {% endblock %}

    {% block body%}

        <div id="main">
            <h1>{{ title }}</h1>

            {% for category in categories %}
               <div> {{ category.title }} </div>
            {% endfor %}
        </div>

    {% endblock %}